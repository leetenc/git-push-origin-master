<h1>Tasks List</h1>
<% @tasks.each do |task| %>
  <p><%= link_to task.title, task_path(task) %> -
     <%= task.completed ? 'Completed' : 'To be done' %>
     <%= link_to '🚮', task_path(task),
            method: :delete,
            data: { confirm: "Are you sure?" }
    %>
  </p>
<% end %>

 <p><%= link_to 'New task', new_task_path %>
